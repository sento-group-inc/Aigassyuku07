# 当日使うプロンプト（コピペ用）

cloneが済んだら、Codex（またはClaude Code）で `Aigassyuku07/kit` フォルダを開き、次を貼ってください。

スキルの呼び方: Codexは `$guide`、Claude Codeは `/guide`。どちらも「guideで」と文章で頼んでも動きます。

## 最初に貼るもの

```text
このフォルダで作業します。guide スキルを使って、docs/guide-steps.md を読み、
いまの状態（どのステップまで終わっているか）を判定してから、
次にやることを1つだけ指示してください。

指示するときは、必ず次を含めてください。
- 使うスキル名（例: dashboard-mvp, dashboard-research）
- 使うモデル（段階が変わるときは切り替え方も）
- 私がやること（手で操作する箇所があれば「どこを押すか」）
- 終わりの状態（何ができていれば次に進んでよいか）

コマンドの実行が必要なときは、私に頼まずにあなたが実行してください。
私は会社の業務ダッシュボードを作りたいです。困りごとのメモは手元にあります。
```

## 段階1 粗く作る（安いモデル）

```text
dev-log で、次の困りごとと帳票の項目名を docs/log.md の最初に書いて。
そのあと dashboard-mvp で、mvp/index.html に触れるMVPを作ってブラウザで開いて。
困りごと: （ここに貼る）
帳票の項目名: （ここに貼る）
```

触って気づいたら、そのまま言えばログに残ります（例:「担当ごとに見たい。ログして」）。

## 段階2 研究する（賢いモデルに切り替えてから）

```text
dashboard-research で、mvp/index.html と docs/log.md を研究して、docs/research.md に残して。
私にしか分からないことは、番号付きの質問にまとめて推奨回答つきで聞いて。
```

## 段階3 計画する（賢いモデルのまま）

```text
dashboard-plan で、docs/research.md と docs/log.md から、ゼロから作り直すための計画一式を作って。
```

## 段階4 ゼロから作り直す（安いモデルに戻してから）

```text
guide で次の一手を教えて。mvp/ のコードは使わず、計画の docs だけを見て作り直して。
```

## モデルを切り替える前

```text
ここまでで出た要望・決定・詰まりを docs/log.md に書いて。次のモデルがファイルだけ読んで続きから始められるようにして。
```

## 各ステップで貼るもの

講義サイトの「作る（10ステップ）」の各Stepに、同じものがコピーボタン付きで載っています: https://aigassyuku07.vercel.app/build

```text
次は何をすればいい？ guide で現在地を判定して、次の一手を1つだけ教えて。コマンドはあなたが実行して。
```

## 進め方が分からなくなったとき

```text
poteto-mode で、いまの状態から足りないものを1つ見つけて、次の一手を理由つきで教えてください。
```

## エラーが出たとき

```text
〇〇をしようとしたら、次のエラーが出ました。原因を1つに絞ってから、直し方を教えてください。
（エラー文をそのまま貼る）
```

## 社内に説明したいとき

```text
eli5 で、このダッシュボードで何が変わるのかを、社長向けに説明してください。
```

## 一日の終わりに貼るもの

```text
今日の結果をまとめてください。
- できたこと（本番URLと、動いている操作）
- できなかったこと、詰まった場所
- 次にやるスライス（docs/roadmap.md に印を付ける）
そのあと reflect で、今日の学びを残す案を出してください。
```
