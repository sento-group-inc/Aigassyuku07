# 事前準備（当日までにやること）

当日は作業時間を最大化したいので、**環境は事前に用意**しておいてください。所要時間は40〜60分です。

詰まったら、そのままにせず事前に連絡してください。当日の午前で拾いますが、事前に潰せた分だけ手を動かせます。

## 1. アカウント（無料枠で始められます）

| サービス | 用途 | 準備 |
|---|---|---|
| GitHub | コードの置き場 | アカウント作成（未取得なら） |
| Vercel | アプリの公開 | GitHubアカウントで連携 |
| Supabase | データベース | アカウント作成（無料枠） |
| TypeSafe (JEV) | 型つきの判断をAIにさせる | APIキーを取得（当日案内します） |

**クレジットカードは当日は不要です。** 無料枠の範囲で作ります。

## 2. ツールのインストール

### 必須

| ツール | 確認コマンド | 期待する結果 |
|---|---|---|
| git | `git --version` | `git version 2.x.x` |
| Node.js（20以上） | `node -v` | `v20.x.x` 以上 |
| GitHub CLI | `gh --version` | `gh version 2.x.x` |
| Codex アプリ | — | 起動してサインインできる |

### Claude を使う場合

Claude Desktop / Claude Code をインストールし、サインインしておいてください。

## 3. リポジトリのclone

```bash
git clone https://github.com/sento-group-inc/Aigassyuku07.git
cd Aigassyuku07/kit
./setup.sh
```

`setup.sh`は、必要なツールの有無を確認し、足りないものを表示します。**何かを勝手にインストールしたり、既存の設定を上書きしたりしません。**

## 4. 当日の持ち物

- PC（充電器）
- **自社の業務の困りごと1つ**（例: 案件の進捗が誰にも見えない、報告が漏れる、紙とExcelが混在している）
- いま使っている帳票やスプレッドシートの**項目名**（顧客名・案件名・工程名など。中身の個人情報は不要）

> 画面を作るのに使うのは**項目名だけ**です。顧客の個人情報や売上の中身は当日は持ち込まないでください。ダミーデータで作ります。

## 5. うまくいかないとき

| 症状 | 対処 |
|---|---|
| `command not found: node` | Node.jsをインストールし、ターミナルを開き直す |
| `gh`で認証を求められる | `gh auth login`を実行（GitHubアカウントで） |
| Codexにサインインできない | アプリを再起動し、ブラウザの別アカウントでログインしていないか確認 |
| 会社のPCでインストールできない | 事前に連絡してください。管理者権限なしで進める手順を用意します |
