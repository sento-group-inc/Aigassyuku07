# 事前準備（当日までにやること）

**Codex／Claude、git、Node.jsのインストールは済んでいる前提**です。ここでは当日必要な「アカウント」と「JEVのAPIキー」を用意します。所要20〜30分。

当日は手を動かす時間を最大化したいので、この4つを済ませてから来てください。

## 1. キットをcloneする（2分）

```bash
git clone https://github.com/sento-group-inc/Aigassyuku07.git
cd Aigassyuku07/kit
./setup.sh
```

`setup.sh`は**何もインストールしません**。足りないものと入手先URLを表示するだけです。

次に、外部サービスの準備状況を確認します。

```bash
./setup.sh --check-services
```

`NG`が出たものを、以下で埋めてください。

## 2. JEVのAPIキーを取る（5分）

**各社で取得**します。合宿用の共通キーは配りません。

1. https://typesafe.ai を開く
2. 右上の`Sign In` → **会社のアカウント**でサインイン（Google連携またはメール）
3. 管理画面でAPIキーを発行し、その場でコピーする
4. キーを保存する（入力は画面に表示されません）

```bash
python3 tools/jev-configure.py
python3 tools/jev-configure.py --verify
```

`接続に成功しました。`と出れば完了です。

> **キーはチャットに貼らないでください。** 会話ログに残ります。

詳しい画面付きの手順: [site/jev.html](site/jev.html) ／ [kit/docs/jev-setup.md](kit/docs/jev-setup.md)

## 3. アカウントを用意する（10分）

| サービス | やること | リンク |
|---|---|---|
| GitHub | アカウント作成 → `gh auth login` | https://github.com/signup |
| Vercel | **GitHubアカウントで**登録 | https://vercel.com/signup |
| Supabase | アカウント作成（無料枠） | https://supabase.com/dashboard |

**クレジットカードは当日不要です。** 無料枠の範囲で作ります。

## 4. 題材のメモを用意する（10分）

当日いきなり自社の題材で始めます。次の2つをメモしてきてください。

**(a) 困りごとを1つ**

> 誰が、何に困っていて、何が見えると嬉しいか

例:

- 案件の進捗が誰にも見えず、毎朝口頭で確認している
- 見積の状況が担当者ごとにバラバラで、止まっている案件に気づけない

**(b) いま使っている帳票の「項目名」**

スプレッドシートや紙の見出しを、そのまま書き写してきてください（顧客名や金額の中身は不要です）。

> 例: 顧客名 / 担当 / 受付日 / 見積金額 / 状況 / 次アクション

**個人情報や実データは持ち込まないでください。** 画面はダミーデータで作ります。

## 当日の持ち物

- PC（充電器）
- 上記のメモ（紙でも可）
- 会社のアカウント情報（JEV・GitHub・Vercel・Supabase）

## うまくいかないとき

| 症状 | 対処 |
|---|---|
| `command not found: node` | Node.jsを入れて、ターミナルを開き直す |
| `gh`で認証を求められる | `gh auth login`を実行する |
| JEVで`HTTP 401`/`403` | キーの値と契約状態を確認。分からなければ作り直して`--replace` |
| JEVで`APIへ接続できませんでした` | ネットワーク。社内プロキシの可能性 |
| 会社のPCで入れられない | 事前に連絡してください。管理者権限なしで進める手順を用意します |

**解決しなくても大丈夫です。** 当日の午前（10:00〜）で拾います。ただし、事前に潰せた分だけ手を動かせます。
