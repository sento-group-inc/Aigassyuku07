---
name: dashboard-keys
description: 外部連携に必要なキーとアカウントを棚卸しし、置き場所を決めるときに使う（実際に作るのはStep 6のdashboard-repo）。
---

# dashboard-keys — キーの棚卸し

たとえるなら、**合鍵の一覧表**。どの扉の鍵が、誰のキーホルダーに付いているかを書く。鍵そのもの（値）は表に貼らない。

## 手順

1. ロードマップを見て、外部と繋がる箇所を全部挙げる
2. 何のサービスに、何のために繋ぐかを書く
3. 必要なキーと、その**取得場所**を書く
4. **置き場所**を決める（ローカルの`.env.local`／ホスティングの環境変数）
5. `docs/keys.md`へ表として書く。**値は書かない**
6. 実際のプロジェクト作成と登録は、Step 6で`dashboard-repo`が行う

## 置き場所の原則

| 種類 | 置き場所 |
|---|---|
| サーバーだけが使う秘密（DBの管理者キーなど） | ホスティングの環境変数 |
| ブラウザに渡ってよいキー（公開キー） | 環境変数（公開前提のものだけ） |
| ローカル開発用 | `.env.local`（gitに入れない） |

## 終わりの状態

- `docs/keys.md`に表があり、値がどこにも書かれていない
- 最低限の2つ（Supabase の Project URL と Publishable key）の置き場所が決まっている

## つまずき

| 症状 | 対処 |
|---|---|
| どのキーが要るか分からない | 最初はSupabaseの2つだけで足りる。他の連携は後で足せる |
| キーを間違ってコミットした | その場で無効化して再発行する。履歴から消すより先に無効化 |
| どこに置くか迷う | ブラウザで見えてよいか？を基準にする。駄目ならサーバー側 |
